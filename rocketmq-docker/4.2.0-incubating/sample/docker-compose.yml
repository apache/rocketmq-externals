version: '2'
services:
  namesrv:
    image: apache/incubator-rocketmq-namesrv:4.2.0-incubating-dev
    container_name: rmqnamesrv
    build:
      context: ./namesrv
      args:
        - version=4.2.0
    ports:
      - 9876:9876
    volumes:
      - ./data/namesrv/logs:/opt/logs
      - ./data/namesrv/store:/opt/store
  broker:
    image: apache/incubator-rocketmq-broker:4.2.0-incubating-compose
    container_name: rmqbroker
    build:
      context: ./broker
      args:
        - version=4.2.0
    ports:
      - 10909:10909
      - 10911:10911
    volumes:
      - ./data/broker/logs:/opt/logs
      - ./data/broker/store:/opt/store
    depends_on:
      - namesrv
